import axios from 'axios'

axios.defaults.baseURL = "http://localhost:8000/api/user/"

describe("Auth routers", () => {
	test("No email should return 401", () => {

		
	})
})

describe("User routers", () => {
	test("Get all users — Should place a valid token", async () => {
	
	})
})